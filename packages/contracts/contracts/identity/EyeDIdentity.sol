// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

/// @title EyeDIdentity - Soulbound Identity Token (Phase 2)
/// @notice ERC-721 + ERC-5192 soulbound token for patient identity
/// @dev Placeholder — full implementation in Phase 2
contract EyeDIdentity {
    // Phase 2: ERC-721 + ERC-5192 soulbound implementation
    // - mint(address to, bytes32 identityHash) → tokenId
    // - burn(tokenId) — patient-initiated deletion
    // - locked(tokenId) → always true (soulbound)
    // - getIdentityHash(tokenId) → bytes32
    // - isEnrolled(identityHash) → bool
}
